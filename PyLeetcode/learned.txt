Rolling hash:

theory:
(a + b) mod Q = (a mod Q + b mod Q) % Q = (a mod Q + b) % Q = (a + b % Q) % Q
(a * b) mod Q = (a mod Q * b mod Q) mod Q
(a ^ b) mod Q = (a mod Q ^ b) mod Q
mod operation is idempotent!!

to prove:
let a = a*x1 + b
let b = a*x2 + c
% by a



1.
12 // 10 floor division

2. ternary condition in python
a = 5 if x == 0 else 78